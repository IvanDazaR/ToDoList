<?php

use Illuminate\Database\Eloquent\Model; // Importando la libreria Eloquent.

class Task extends Model{
    protected $table = 'tasks'; // como practica le ponemos la tabla que queremos que utilice 
}